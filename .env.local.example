# Postgres (self-hosted on the VDS)
DATABASE_URL=

# Scoring service (FastAPI on the VDS)
SCORING_SERVICE_URL=
# Shared secret the web app sends to the scoring service as auth_token.
SCORING_WEBHOOK_SECRET=
